# sd-webui-language-diffusion has no third-party Python dependencies. The
# extension's runtime Python lives in scripts/language_diffusion_setup.py,
# which only touches `modules.shared` — already shipped by Forge / A1111.
# This file exists so the Extensions tab surfaces the extension cleanly.
